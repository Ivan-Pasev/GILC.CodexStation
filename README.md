# GILC CodexStation Ω

Reference specification and implementation surface for **GILC CodexStation Ω**.

## Current frontier

- **Ω100** — executable carrier/runtime predecessor baseline; historical status remains evidence-bound.
- **Ω101** — Cognitive Runtime Genesis; active implementation frontier.
- **Ω101 is not globally PASS** until acceptance gates `G101-01..G101-13` carry explicit witness evidence.

## Constitutional separation

`Cognition != Authority != Observer != Station != Model/Service`

A model is a replaceable reasoning service, not canonical entity identity. Initiative or necessity never creates authority. Raw model output cannot directly self-authorize canonical memory admission.

## Canonical authority

The authoritative constitutional working tree is the Google Drive root:

`GILC_CODEX_STATION_OMEGA_CANONICAL_WORKING_TREE`

Repository releases implement approved canonical state; they do not silently redefine it.

## Quick start

```bash
python -m venv .venv
pip install -e ".[dev]"
pytest
```

## Ω101 acceptance gates

`G101-01` deterministic genesis identity  
`G101-02` separate station/entity roots  
`G101-03` typed memory operational  
`G101-04` provenance admission enforced  
`G101-05` self/world separation enforced  
`G101-06` KBI cognitive kernel operational  
`G101-07` MLCO role orchestration operational  
`G101-08` authority lattice operational  
`G101-09` witnessed effect transactions  
`G101-10` homeostasis operational  
`G101-11` portable re-instantiation verified  
`G101-12` adversarial suite PASS  
`G101-13` Ω100 invariants preserved
